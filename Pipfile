[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
numpy = ">=*"
requests = ">=*"
mtcnn = ">=0.1.0"
opencv-python = ">=4.3.0.36"
scikit-learn = ">=0.23.2"
tensorflow = ">=2.2.0"
tensorflow-gpu = ">=2.2.0"
frsystem = {editable = true,path = "."}

[requires]
python_version = "3.7"
